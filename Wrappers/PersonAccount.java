class PersonAccount {
    String name;      // Имя и фамилия
    double moneyRUB;  // Счёт в рублях
    double moneyUSD;  // Счёт в долларах
    double moneyEUR;  // Счёт в евро
    Person.Account(moneyRUB, moneyUSD, money UER)


    PersonAccount(String userName, Double rub, Double usd, Double eur) {
        name = userName;
        // rub = moneyRUB;
        // usd = moneyUSD;
        // eur = moneyEUR;
    }
}